# คู่มือสำหรับผู้พัฒนา / Working notes

บันทึกสิ่งที่ต้องรู้ก่อนแก้โค้ดนี้ เพื่อไม่ต้องไล่อ่านใหม่ทุกครั้ง
Notes for whoever (or whatever) works on this next.

## ข้อควรระวังก่อนเริ่ม (Read this first)

- **แอปนี้ใช้งานจริงอยู่** ครูทั้งโรงเรียนใช้ทุกวัน push ขึ้น `main` = deploy
  ขึ้น GitHub Pages ทันที (`.github/workflows/deploy.yml`)
- **`npm run dev` เขียนลงฐานข้อมูลจริง** ไม่มี staging/emulator แยก ระวังเวลา
  ทดลอง
- CI รัน `npm run lint` (`eslint .` — รวมไฟล์เทสต์ด้วย) แล้วจึง build
  **รัน `npm run lint` เองก่อน push ทุกครั้ง** โดยเฉพาะเมื่อเพิ่งเพิ่มไฟล์ใหม่
  (เคยพลาดมาแล้ว: ใช้ `Buffer` ในไฟล์เทสต์ ซึ่ง config ฝั่งเบราว์เซอร์ไม่รู้จัก
  lint ที่รันไปก่อนหน้ายังไม่เห็นไฟล์นั้น → deploy พัง)
- ข้อความที่ผู้ใช้เห็นเป็นภาษาไทยทั้งหมด คอมเมนต์ในโค้ดเป็นภาษาอังกฤษ

## โครงสร้างข้อมูล (Firestore layout)

ทุกอย่างอยู่ใน collection เดียวคือ `system_db` (เพื่อให้ Firestore rules
`match /system_db/{docId}` ครอบคลุมได้หมด) — `src/db.js` เป็นชั้นเดียวที่คุยกับ
Firestore

| เอกสาร | เก็บอะไร | โหลดเมื่อไร |
|---|---|---|
| `teachers` | รายชื่อครูทั้งหมด + `profilePicture` (base64) | ทุกครั้งที่เปิดแอป |
| `supervisions` | การนิเทศ **ของปีการศึกษาปัจจุบัน** + `archivedYears` | ทุกครั้งที่เปิดแอป |
| `supervisions_<ปี>` | การนิเทศของปีที่จบไปแล้ว | ทุกครั้งที่เปิดแอป (ปีละ 1 read) |
| `term_plans` | แผนการสอนรายภาคเรียน (ไม่รวมไฟล์) | ทุกครั้งที่เปิดแอป |
| `settings` | ปีการศึกษา ตำแหน่ง กลุ่มสาระ กลุ่ม PLC | เมื่อเปิดแอป |
| `plclog_<id>` | บันทึก PLC 1 ใบ (ไม่รวมรูป) | เมื่อเข้าแท็บ PLC |
| `plcimg_<logId>` | รูปของบันทึก PLC ใบนั้น | เมื่อเปิดดูบันทึกนั้น |
| `evalimg_<supId>_<ผู้นิเทศ>` | รูปประเมินนิเทศของผู้นิเทศ 1 ท่าน | เมื่อเปิดดู/พิมพ์รายงาน |
| `onepage_<supId>` | ไฟล์นิเทศหน้าเดียว (ภาพ/PDF) | เมื่อกดเปิดดู |
| `postlesson_<planId>` | ไฟล์ PDF บันทึกหลังแผน | เมื่อกดเปิดดู |
| `*_migration` | ตัวบอกว่าย้ายข้อมูลชุดนั้นเสร็จแล้ว | ครั้งเดียว |

## กฎเหล็ก: อย่าเก็บไฟล์ base64 ไว้ในเอกสารที่รวมเป็น array

ทุกเคส "บันทึกข้อมูลไม่ได้" ในโปรเจกต์นี้มาจากเรื่องเดียวกัน — เอาไฟล์ base64
(รูป/PDF) ยัดไว้ในเอกสารที่เก็บรวมเป็น array จนชนเพดาน `MAX_DOC_BYTES`
(900KB กันไว้จากลิมิตจริง 1MiB ของ Firestore) แล้วเขียนอะไรไม่ได้อีกเลย

แยกออกมาแล้วทั้งหมด (ก.ย. 2569 / Sept 2026):
บันทึก PLC → รายใบ, รูปประเมิน → รายผู้นิเทศ, ไฟล์นิเทศหน้าเดียว, ไฟล์บันทึก
หลังแผน, รูป PLC และการแยก `supervisions` ตามปีการศึกษา

**ถ้าจะเพิ่มการแนบไฟล์ใหม่:** ใช้ `attachmentStore(prefix, ชื่อไฟล์ในข้อความ error)`
ใน `src/db.js` เก็บเฉพาะ metadata (type/ลิงก์/วันที่/จำนวน) ไว้ในเอกสารหลัก
แล้วโหลดไฟล์จริงตอนผู้ใช้กดเปิดเท่านั้น

**ก่อนเดา ให้วัดก่อน** — วิธีที่ได้ผลทุกครั้ง: เขียนไฟล์ `.mjs` ชั่วคราวใน
รากโปรเจกต์ (ต้องอยู่ในนี้ Node ถึงจะหา `firebase` เจอ) ดึงทุกเอกสารใน
`system_db` มาวัดขนาดทีละอัน แล้วลบไฟล์ทิ้ง

```js
const size = (v) => new TextEncoder().encode(JSON.stringify(v)).length;
const snap = await getDocs(collection(db, 'system_db'));
snap.docs.map(d => [d.id, size(d.data())]).sort((a,b) => b[1]-a[1]).forEach(...)
```

## การแยก supervisions ตามปีการศึกษา

`src/utils/supervisionShards.js` (ตรรกะล้วน มีเทสต์) + ส่วนที่คุย Firestore ใน
`src/db.js`

- เอกสาร `supervisions` เก็บเฉพาะปีที่กำลังดำเนินอยู่ ปีที่จบแล้วจะถูกย้ายไป
  `supervisions_<ปี>` อัตโนมัติในการโหลดครั้งแรกของแต่ละ session
  (ดูจาก `settings.currentAcademicYear`) ย้ายใน transaction เดียว
- อ่าน: `readSupervisionShards()` รวมทุกปีกลับมาเป็น array เดียว
  **ส่วนอื่นของแอปไม่รู้เรื่องการแยกเลย**
- เขียน: `mutateCollection('supervisions', fn)` → `mutateSupervisionShards()`
  ซึ่งเขียนกลับไปยังเอกสารที่รายการนั้นอยู่จริง
- ถ้าลบรายการจนปีนั้นว่าง เอกสารปีนั้นจะถูกลบและตัดออกจาก `archivedYears`

## การดาวน์โหลดไฟล์

- `src/utils/fileDownload.js` — ตั้งชื่อไฟล์, อ่านนามสกุลจาก data URL, `saveBlob`
- `src/utils/zipDownload.js` — รวมเป็น ZIP **แยกโฟลเดอร์ตามชื่อครู**
  (`โฟลเดอร์ครู/ชื่อไฟล์.jpg`) ชื่อซ้ำในโฟลเดอร์เดียวกันจะต่อท้าย `_2`
  ไม่ทับกัน, ไม่บีบอัดซ้ำ (`STORE`) เพราะรูปกับ PDF บีบมาแล้ว,
  โหลด JSZip แบบ dynamic import → แยกเป็น chunk ~96KB โหลดเฉพาะตอนกดปุ่ม
- ปุ่มทั้งหมดได้ผลเป็น ZIP: รายคน (การ์ดวงรอบ 3 ของครู, หน้าต่างรายงานและ
  หน้าต่างรายละเอียดของแอดมิน) และทั้งปี (หัวแท็บ "รายงานผล PLC" ของแอดมิน)
- ภาพ "PLC วงรอบที่ 3" คือรูปประเมินจาก `evalimg_*` ของการนิเทศ ไม่ใช่รูปจาก
  บันทึก PLC (วงรอบ 3 ไม่มีบันทึก PLC ของตัวเอง — ระบบสร้างขึ้นจากการนิเทศ)
- รายงานที่แนบเป็น "ลิงก์" ดาวน์โหลดแทนไม่ได้ ต้องบอกจำนวนให้ผู้ใช้รู้

## การเขียนเทสต์

- `npm test` (vitest) — ตรรกะล้วนอยู่ใน `src/utils/*.test.js`
- **ทดสอบกับฐานข้อมูลจริงได้** โดยสร้างไฟล์ `.test.js` ชั่วคราวใน `src/`
  (ต้องอยู่ใน `src/` เพราะ import แบบไม่มีนามสกุล ต้องพึ่ง resolver ของ Vite)
  ใส่ `globalThis.localStorage` ปลอม แล้ว `await import('./db.js')` ใช้ของจริง
  ได้เลย — ใช้ข้อมูลทดสอบที่ลบทิ้งเมื่อจบ แล้วเช็คว่าฐานข้อมูลกลับสภาพเดิม
  **ลบไฟล์เทสต์ชั่วคราวทิ้งทุกครั้ง** อย่า commit
- ระวังลำดับ: อย่ารัน migration บนฐานข้อมูลจริงก่อน deploy โค้ดใหม่ ไม่งั้น
  ผู้ใช้ที่ยังเปิดโค้ดเก่าอยู่จะมองไม่เห็นข้อมูลที่ย้ายไปแล้ว

## จุดที่ยังต้องจับตา (ตรวจล่าสุด ก.ย. 2569 / Sept 2026)

- **`evalimg_*` ใบใหญ่สุด 663KB (74% ของเพดาน)** — 137 รูป รวม 10MB, ใหญ่สุด
  191KB, มัธยฐาน 61KB **ตัดสินใจแล้วว่าไม่บีบอัดซ้ำ** เพราะเป็นภาพหลักฐาน
  การนิเทศ คุณภาพที่เสียไปเรียกคืนไม่ได้ และเพดานไม่เสี่ยงจริง (จำกัด 4 รูป
  ต่อผู้นิเทศต่อการนิเทศ ของใหม่บีบเหลือ ~47KB) ถ้าจะรื้อเรื่องนี้อีก ต้องถาม
  เจ้าของงานก่อน
- **`teachers` 38.3KB** (มีรูปโปรไฟล์ 4/33 คน) ตอนนี้จำกัดรูปละ 10KB แล้ว
  (`resizeImage(..., { maxBytes })`) กรณีแย่สุดถ้าครูใส่รูปครบทุกคน ~340KB
  (38% ของเพดาน) ไม่โตตามเวลา แต่โหลดทุกครั้งที่เปิดแอป ถ้าวันหนึ่งอยากลด
  น้ำหนักหน้าแรกจริงจัง ต้องแยกรูปโปรไฟล์ออกไปอีกเอกสาร แล้วโหลดทีหลัง
- **`plc_logs` ลบไปแล้ว** (ก.ย. 2569) หลังตรวจว่าบันทึกทั้ง 3 ใบในนั้นมีอยู่ครบ
  ใน `plclog_*` สำรองไว้ที่ `backup/system_db_plc_logs_2026-09-08.json`
  (โฟลเดอร์ `backup/` อยู่ใน .gitignore) โค้ด `migrateLegacyPlcLogs()` ถูกลบ
  ไปพร้อมกัน — ประหยัด 1 read ทุกครั้งที่เปิดแท็บ PLC

## Firestore rules — ยังไม่แก้ (ตัดสินใจเมื่อ ก.ย. 2569)

ฐานข้อมูล**เปิดกว้าง**: ใครก็ตามที่หยิบ firebaseConfig จากไฟล์ JS ที่ deploy
ไปแล้ว อ่าน/เขียน `system_db` ได้ทั้งหมด รวมถึง bcrypt hash รหัสผ่านครู
(ยืนยันแล้วด้วยการอ่านเขียนจากเครื่องนอกแอป) เจ้าของงานเลือก "ยังไม่แก้ตอนนี้"
เพราะข้อมูลไม่ลับมาก และทางแก้ที่ดีต้องใช้เวลา — อย่าเพิ่งไปแตะเองโดยไม่ถาม

สรุปทางเลือกที่วิเคราะห์ไว้แล้ว (จะได้ไม่ต้องคิดใหม่):

1. **Anonymous auth + rules `request.auth != null`** — ฟรี ทำได้เร็ว แต่กัน
   ได้แค่การเข้าถึงแบบตรง ๆ เพราะคนที่ตั้งใจก็ขอ anonymous token เองได้
   ต้องเปิด provider Anonymous ใน Firebase Console เอง (CLI เปิดให้ไม่ได้)
2. **Firebase Auth เต็มรูปแบบต่อรายครู** — ย้ายบัญชีได้จริงด้วย
   `firebase auth:import --hash-algo=BCRYPT` (Firebase รองรับ bcrypt ตรง ๆ
   รหัสผ่านเดิมของครูใช้ต่อได้) ล็อกอินเปลี่ยนไปใช้อีเมลสังเคราะห์เช่น
   `username@kss.local` **ติดตรงที่**: แอดมินตั้ง/เปลี่ยนรหัสผ่านให้ครูคนอื่น
   จากฝั่งเบราว์เซอร์ไม่ได้ (client SDK ทำได้แค่กับบัญชีตัวเอง) ต้องมี Cloud
   Functions + Admin SDK ซึ่งต้องอัปเกรดโปรเจกต์เป็นแผน Blaze (ผูกบัตร แต่
   ปริมาณใช้งานระดับนี้ค่าใช้จ่ายจะเป็น 0)
3. คงสภาพเดิม

หมายเหตุ: `firebase` CLI ในเครื่องนี้ล็อกอินไว้แล้วและเห็นโปรเจกต์
`kss-teaching-supervision` จึง **deploy rules ได้ทันทีเมื่อตัดสินใจ** — แต่
อย่า deploy rules ที่ต้องใช้ auth ก่อนที่ฝั่งแอปจะมี auth จริง ไม่งั้นแอปจะ
ใช้งานไม่ได้ทั้งระบบ
